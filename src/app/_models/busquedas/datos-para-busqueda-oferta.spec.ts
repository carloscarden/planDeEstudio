import { DatosParaBusquedaOferta } from './datos-para-busqueda-oferta';

describe('DatosParaBusquedaOferta', () => {
  it('should create an instance', () => {
    expect(new DatosParaBusquedaOferta()).toBeTruthy();
  });
});
